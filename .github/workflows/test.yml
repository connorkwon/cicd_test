name: CI
on:
  push:
    branches: [ "main" ]
jobs:
  build:
    # The type of runner that the job will run on
    runs-on: ["self-hosted", "macOS"]
    steps:
      - name: Run
        run: echo Hello
        shell: bash
